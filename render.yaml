services:
  - type: web
    name: ecoco-faq-assistant
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn faq_server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
